#!/bin/bash

#$ -cwd               # Run in the current working directory.
#$ -j y               # Direct stdout and stderr to the same file.
#$ -V                 # Use environment variables.
#$ -l mem_free=<%= resources[["mem_free"]] | 3 %>G,h_vmem=<%= resources[["mem_free"]] | 3 %>G
#$ -N <%= job.name %> # job name
#$ -o <%= log.file %> # log file

module load R/4.0.3   
module load fsl
module load java

Rscript -e 'batchtools::doJobCollection("<%= uri %>")' # Leave alone.

exit 0 # Leave alone.
